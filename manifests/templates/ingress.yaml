---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boot                  # Ingress name
  labels:
    app: boot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "boot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: boot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
